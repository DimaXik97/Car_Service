export const selectWorker= (worker)=>({
    type: 'SELECT_WORKER',
    selectWorker: worker
})
export const selectDate= (date)=>({
    type: 'SELECT_DATE',
    selectDate: date
})
export const selectTime= (time)=>({
    type: 'SELECT_TIME',
    selectTime: time
})